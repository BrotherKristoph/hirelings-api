{
  "name": "hirelings-api",
  "version": "0.0.1",
  "description": "Back-end for the Hirelings App",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "firebase deploy",
    "deploy:firestore": "firebase deploy --only firestore",
    "deploy:firestore:rules": "firebase deploy --only firestore:rules",
    "deploy:storage": "firebase deploy --only storage"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/BrotherKristoph/hirelings-api.git"
  },
  "author": "Chris Kraljevic",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/BrotherKristoph/hirelings-api/issues"
  },
  "homepage": "https://github.com/BrotherKristoph/hirelings-api#readme",
  "devDependencies": {
    "firebase-tools": "^3.18.4",
    "firestore-security-tests": "^1.3.1"
  },
  "dependencies": {
    "firebase-admin": "^5.12.0"
  }
}
